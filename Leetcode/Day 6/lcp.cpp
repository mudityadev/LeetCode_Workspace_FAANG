#include<bits/stdc++.h>
using namespace std;

string prefix_test(vector<string>& s){
    string str = "";
    sort(s.begin(), s.end());
    int n = s[0];
    for (int i = 0; i < s.size; i++)
    {
        // i...2
        for (int k = 0; k < n; k++)
        {
            if(s[i][k] == )
        }
        
        /* code */
    }
    
}

int main()
{
    vector<string> s{"apple", "apps", "ape"};
    // cout << prefix_test(s) << " ";
    cout << s[0][0] << " ";

}