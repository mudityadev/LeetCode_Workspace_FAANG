#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main()
{
    fastio;
    vector<int> v;
    vector<int> v1(10);
    vector<char> v2(10,'m');

    for(auto x : v1) cout << x << " ";
    return 0;
}